# Archive

- `starter_pack/`: scaffold files preserved for history after the active codebase was cleaned up.
- `imports/`: legacy duplicate assets and repo snapshots moved out of the working tree.

Nothing here should be treated as part of the active forecasting pipeline.
